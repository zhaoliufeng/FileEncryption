<Window x:Class="FileEncryption.SetOutputPathWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FileEncryption"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Title="配置输出路径" Height="150" Width="260">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="0.3*"/>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>

        <TextBox Grid.Column="0" Grid.Row="1" Width="220" Height="30" VerticalContentAlignment="Center" Name="outPathTextBox"/>

        <Button Grid.Column="0" Grid.Row="2" Width="100" Height="30" Content="确定" Click="OnPathSave"/>
    </Grid>
</Window>
